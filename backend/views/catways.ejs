<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Gestion des catways</title>
  <style>
    table { border-collapse: collapse; width: 100%; margin-top: 20px; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    th { background: #f2f2f2; }
    form { margin: 0; display: inline; }
  </style>
</head>
<body>

<h1>Gestion des catways</h1>

<% if (error) { %>
  <p style="color:red"><%= error %></p>
<% } %>

<h2><%= editCatway ? "Modifier un catway" : "Créer un catway" %></h2>

<form method="POST"
  action="<%= editCatway
    ? '/dashboard/catways/edit/' + editCatway._id
    : '/dashboard/catways' %>">

  <% if (!editCatway) { %>
    <label>Numéro</label><br>
    <input type="number" name="catwayNumber" required><br><br>

    <label>Type</label><br>
    <select name="catwayType" required>
      <option value="short">Short</option>
      <option value="long">Long</option>
    </select><br><br>
  <% } %>

  <label>État</label><br>
  <textarea name="catwayState" rows="3" required><%= editCatway ? editCatway.catwayState : "" %></textarea><br><br>

  <button type="submit"><%= editCatway ? "Modifier" : "Créer" %></button>
</form>

<hr>

<h2>Liste des catways</h2>

<table>
  <thead>
    <tr>
      <th>Numéro</th>
      <th>Type</th>
      <th>État</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  <% catways.forEach(c => { %>
    <tr>
      <td><%= c.catwayNumber %></td>
      <td><%= c.catwayType %></td>
      <td><%= c.catwayState %></td>
      <td>
        <a href="/dashboard/catways/edit/<%= c._id %>">✏️ Modifier</a>
        |
        <form method="POST" action="/dashboard/catways/delete/<%= c._id %>">
          <button type="submit">🗑️ Supprimer</button>
        </form>
      </td>
    </tr>
  <% }) %>
  </tbody>
</table>

<p><a href="/dashboard">⬅ Retour au dashboard</a></p>

</body>
</html>

